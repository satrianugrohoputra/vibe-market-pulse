# ============================================================
# Vibe Market Pulse — Requirements
# Python 3.11 required (see .python-version)
#
# ROOT CAUSE FIX (2025-05):
#   Python 3.14 breaks protobuf + pydantic-v1 with:
#   "TypeError: Metaclasses with custom tp_new are not supported"
#   Fix: pin Python=3.11 via .python-version +
#        pin all dependency versions below.
# ============================================================

# --- Core Streamlit & data ---
streamlit>=1.32.0,<2.0.0
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0
plotly>=5.18.0,<6.0.0
scikit-learn>=1.3.0,<2.0.0

# --- Google Gemini SDK ---
google-genai>=1.0.0,<2.0.0

# --- Protobuf: must stay on 4.x line for Python 3.11.
#     protobuf 5.x introduced the tp_new metaclass change
#     that breaks on older pydantic internals used by chromadb.
protobuf>=4.21.0,<5.0.0

# --- Agentic RAG stack ---
# chromadb 0.6.x is the last line that still ships the
# pure-Python EphemeralClient path AND works on Python 3.11
# without requiring the Rust-based pydantic v2 core.
chromadb>=0.5.0,<0.7.0

# sentence-transformers 3.x requires torch which Streamlit
# Cloud provisions separately; pin to 2.x for lighter install.
sentence-transformers>=2.7.0,<3.0.0

# langgraph: stable 0.2.x line
langgraph>=0.2.0,<1.0.0
